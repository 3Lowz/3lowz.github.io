Mono tech-stack chip used throughout the resume to label technologies. Optional leading devicon/bootstrap icon.

```jsx
<Tag icon={<i className="devicon-typescript-plain colored" />}>TypeScript</Tag>
<Tag tone="accent">React</Tag>
<Tag tone="outline" size="sm">Redis</Tag>
```

Tones: `default | accent | outline`. Sizes: `sm | md`.
