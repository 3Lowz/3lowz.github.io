Opens a page section with a mono eyebrow + large display title.

```jsx
<SectionHeading index="03" eyebrow="Experience" title="Where I've shipped" />
```

Composes `Eyebrow`. `align`: `left | center`.
